[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "iocvault"
version = "0.1.0"
description = "Offline IOC normalization utilities"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["iocvault"]
